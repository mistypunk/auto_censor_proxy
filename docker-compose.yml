services:
  nudenet-api:
    image: ghcr.io/notai-tech/nudenet:latest
    container_name: nudenet_ai
    ports:
      - "8080:8080"
    environment:
      - NUDENET_BATCH_SIZE=1
    privileged: true
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
